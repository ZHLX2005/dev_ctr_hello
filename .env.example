# Service port
PORT=8080

# Authentication token (change this to a secure random string)
AUTH_TOKEN=your-secret-token-change-me

# Default file expiration time (formats: 1h, 30m, 24h, etc)
DEFAULT_TTL=1h

# Storage directory (container path)
STORAGE_DIR=/app/storage
